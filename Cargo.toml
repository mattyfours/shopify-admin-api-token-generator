[package]
edition = "2024"
name = "ShopifyAdminTokenGenerator"
version = "0.1.0"

[dependencies]
inquire = "0.7.5"
reqwest = {version = "0.11", features = ["json"]}
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"
tokio = {version = "1", features = ["full"]}
urlencoding = "2"
